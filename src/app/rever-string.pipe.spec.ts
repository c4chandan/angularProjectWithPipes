import { ReverStringPipe } from './rever-string.pipe';

describe('ReverStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ReverStringPipe();
    expect(pipe).toBeTruthy();
  });
});
